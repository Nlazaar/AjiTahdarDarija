import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  // TODO: implement signup, signin, token generation (JWT)
  async validateUser(/* credentials */) {
    return null;
  }
}
