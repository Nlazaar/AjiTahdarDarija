PROMPT PHASE 20 — Stabilisation backend (à coller dans /backend)
Code
Je veux stabiliser et optimiser tout le backend NestJS avant la V1.

Fais les actions suivantes :

1. Ajouter une validation globale :
   - class-validator
   - class-transformer
   - ValidationPipe global dans main.ts

2. Ajouter un middleware global de logging :
   - logs pour chaque requête (méthode, URL, durée)

3. Ajouter un middleware global d’erreurs :
   - catcher toutes les exceptions
   - retourner un format JSON propre :
       { statusCode, message, timestamp, path }

4. Ajouter un rate-limiter simple :
   - limiter les requêtes par IP
   - protéger les endpoints sensibles

5. Ajouter un module HealthCheck :
   - GET /health → retourne :
       { status: "ok", uptime, db: "connected" }

6. Ajouter un module Metrics :
   - compteur de requêtes
   - temps moyen de réponse
   - erreurs par minute

7. Ajouter des tests unitaires pour :
   - auth
   - lessons
   - gamification
   - review

8. Nettoyer le code :
   - supprimer les imports inutiles
   - organiser les modules
   - ajouter des commentaires clairs
